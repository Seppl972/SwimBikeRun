<UserControl x:Class="SwimBikeRun.Views.WorkoutDetailView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    
    <!-- Detailansicht eines Workouts: Alle Informationen zu einem Workout anzeigen -->
    <StackPanel Margin="20" Background="White">
        <TextBlock Text="Workout Details" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>
        <TextBlock Text="{Binding Datum, StringFormat=Datum: dd.MM.yyyy}" FontSize="14" Margin="0,0,0,10"/>
        <TextBlock Text="{Binding Sportart, StringFormat=Sportart: {0}}" FontSize="14" Margin="0,0,0,10"/>
        <TextBlock Text="{Binding DauerMinuten, StringFormat=Dauer: {0} Minuten}" FontSize="14" Margin="0,0,0,10"/>
        <TextBlock Text="{Binding DistanzKm, StringFormat=Distanz: {0} km}" FontSize="14" Margin="0,0,0,10"/>
        <TextBlock Text="{Binding DurchschnittsPace, StringFormat=Durchschnitts-Pace: {0} km/h}" FontSize="14" Margin="0,0,0,10"/>
        <TextBlock Text="{Binding Notiz, StringFormat=Notiz: {0}}" FontSize="14" Margin="0,0,0,20"/>
        <Button Content="Zurück zur Liste" Command="{Binding ZurückCommand}" Width="150" Padding="10"/>
    </StackPanel>
</UserControl>
